<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Nhi khùn💩</title>
  <style>
  body {
    margin: 0;
    padding: 0;
    overflow: hidden;
    font-family: 'Arial', sans-serif;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: url('https://i.postimg.cc/XJgCxjfn/nyfast.jpg');
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
  }
    #startButton {
      padding: 15px 30px;
      font-size: 18px;
      background-color: #f589f5;
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      box-shadow: 0 4px 15px rgba(12, 11, 11, 0.2);
      transition: transform 0.3s, background-color 0.3s;
      z-index: 10;
    }

    #startButton:hover {
      transform: scale(1.1);
      background-color: #ff3399;
    }

    .popup {
      position: absolute;
      width: 220px;
      height: 110px;
      background-color: rgba(255, 240, 245, 0.9);
      border: 1px solid #ff99cc;
      border-radius: 10px;
      box-shadow: 2px 2px 15px rgba(0, 0, 0, 0.2);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-size: 15px;
      animation: fadeIn 0.5s ease;
      z-index: 5;
    }

    .popup h3 {
      margin: 0;
      color: #ff3399;
    }

    .popup p {
      margin: 5px 0 0;
      color: #cc3366;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.8); }
      to { opacity: 1; transform: scale(1); }
    }

    .heart, .poop {
      position: fixed;
      width: 25px;
      height: 25px;
      animation: fall 6s linear infinite;
      z-index: 1;
    }

    .heart {
      background-color: pink;
      transform: rotate(45deg);
    }

    .heart::before,
    .heart::after {
      content: '';
      position: absolute;
      width: 25px;
      height: 25px;
      background-color: pink;
      border-radius: 50%;
    }

    .heart::before {
      top: -12px;
      left: 0;
    }

    .heart::after {
      top: 0;
      left: -12px;
    }

    .poop img {
      width: 100%;
      height: 100%;
    }

    @keyframes fall {
      0% {
        top: -10%;
        opacity: 1;
      }
      100% {
        top: 110%;
        opacity: 0;
      }
    }

    audio {
      display: none;
    }
  </style>
</head>
<body>
  <button id="startButton">Nhấn dô đi 💩</button>
  <audio id="bgMusic" src="pia.mp3.mp3" loop></audio>
></audio>

  <script>
    function createPopup() {
      const popup = document.createElement('div');
      popup.classList.add('popup');
      popup.style.top = Math.random() * (window.innerHeight - 110) + 'px';
      popup.style.left = Math.random() * (window.innerWidth - 220) + 'px';
      popup.innerHTML = `
        <h3>Nhi khùn Nhi khùn!</h3>
      `;
      document.body.appendChild(popup);
      setTimeout(createPopup, 350);
    }

    function createHeart() {
      const heart = document.createElement('div');
      heart.classList.add('heart');
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.animationDuration = (Math.random() * 3 + 3) + 's';
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 6000);
    }

    function createPoop() {
      const poop = document.createElement('div');
      poop.classList.add('poop');
      poop.style.left = Math.random() * 100 + 'vw';
      poop.style.animationDuration = (Math.random() * 4 + 3) + 's';
      poop.innerHTML = '<img src="poop.png" alt="💩">';
      document.body.appendChild(poop);
      setTimeout(() => poop.remove(), 7000);
    }

    document.getElementById('startButton').addEventListener('click', () => {
      createPopup();
      setInterval(createHeart, 200);
      setInterval(createPoop, 350);
      document.getElementById('bgMusic').play();
      document.getElementById('startButton').remove();
    });
  </script>
</body>
</html>
